#!/usr/bin/env bash
# Exécute une commande dans le conteneur de construction, depuis la racine du
# dépôt, quel que soit le répertoire courant. `bin/dans ./gradlew tasks`.
set -euo pipefail
cd "$(dirname "$0")/.."
exec docker compose run --rm android "$@"
